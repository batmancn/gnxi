
syntax = "proto3";

// Path:
// interfaces/interface/ethernet[name=Ethernet16]/state/ipv4/addresses

package mt.proto;

message AddressesAddress {
    string addr = 1;
}

message Ipv4Addresses {
    repeated AddressesAddress addresses = 1;
}